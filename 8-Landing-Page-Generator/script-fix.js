// Landing Page Generator - FIXED VERSION
// Ten plik zawiera naprawioną wersję generatora

// Po prostu skopiuj content z oryginalnego pliku do linii 1029
// A potem dodaj tylko te 4 linie:

// E-commerce Template
function getEcommerceTemplate() {
    return getModernTemplate(); // Uses modern template as base
}

// Agency Template
function getAgencyTemplate() {
    return getGradientTemplate(); // Uses gradient template as base
}

console.log('✅ Landing Page Generator działa poprawnie!');
console.log('Dostępne szablony: modern, minimal, gradient, tech, business, saas, ecommerce, agency');
